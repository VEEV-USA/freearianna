import styled from "styled-components";

const AriannaMainWrap = styled.div`
  padding: 52px 0;
  path {
    pointer-events: all;
  };
  path:hover {
    opacity: 0.5;
    cursor: pointer;
  };
  
`

export default AriannaMainWrap