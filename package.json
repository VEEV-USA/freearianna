{
  "private": true,
  "name": "test-realm",
  "scripts": {
    "start": "npx wrangler@beta dev --local src/index.ts",
    "build": "worktop build src/index.ts",
    "types": "tsc --noEmit --skipLibCheck"
  },
  "dependencies": {
    "itty-router": "^2.6.1",
    "realm-web": "^1.5.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "3.1.1",
    "typescript": "4.4.3",
    "worktop.build": "0.0.3"
  }
}
